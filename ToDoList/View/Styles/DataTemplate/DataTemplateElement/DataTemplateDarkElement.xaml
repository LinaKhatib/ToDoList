<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/View/Styles/TextBoxStyle.xaml"/>
        <ResourceDictionary Source="/View/Styles/ButtonStyles/ButtonDelete.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <DataTemplate x:Key="DataTemplateDarkElement">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>

            <CheckBox IsChecked="{Binding IsDone, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{DynamicResource CheckBoxElementDark}" Content="✔"/>
            
            <TextBox Grid.Column="1" Text="{Binding ThingName, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                     Style="{StaticResource TextBox1}" Foreground="#966B6B" FontSize="36" Margin="20,0,0,0"/>

            <Button Grid.Column="2"
                    Style="{StaticResource ButtonDelete}"
                    Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                    CommandParameter="{Binding}" >
                <Image Source="/View/Images/CrossDark.png" Width="35" Height="35"/>
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>