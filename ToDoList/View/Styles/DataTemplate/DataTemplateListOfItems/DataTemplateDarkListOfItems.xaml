<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/View/Styles/ButtonStyles/ButtonDelete.xaml"/>
    </ResourceDictionary.MergedDictionaries>
    <DataTemplate x:Key="DataTemplateDarkListOfItems">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="90"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding CollectionName, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" />
            <Image Grid.Column="1" Source="/View/Images/SeparatorDark.png"/>
            <TextBlock Grid.Column="2" Text="{Binding Date}" VerticalAlignment="Center" />


            <Button Grid.Column="3" 
                Style="{StaticResource ButtonDelete}"
                Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                CommandParameter="{Binding}">
                <Image Source="/View/Images/CrossDark.png" Width="35" Height="35"/>
            </Button>
        </Grid>
    </DataTemplate>
</ResourceDictionary>