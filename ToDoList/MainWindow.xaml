<Window x:Class="ToDoList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ToDoList"
        mc:Ignorable="d"
                Title="MainWindow" Height="767" Width="1189" AllowsTransparency="True" Background="Transparent"  WindowStyle="None">

    <Border Background="#FFFFEBEB" CornerRadius="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource ButtonControlAndNavigation}">
                    <Image Source="/View/Images/Back.png"/>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource ButtonControlAndNavigation}">
                    <Image Source="/View/Images/Menu.png"/>
                </Button>
            </Grid>

            <ListBox x:Name="ListItemTopics" Grid.Row="1" Background="Transparent" BorderBrush="Transparent" 
                     HorizontalAlignment="Center" ScrollViewer.CanContentScroll="False"
                     ScrollViewer.VerticalScrollBarVisibility="Hidden"
                     ItemsSource="{Binding CollectionsOfLists}">
                <ListBoxItem Style="{StaticResource ListBoxItemForListItems}">
                    <Button x:Name="ButtonStyleInListBoxItem" Style="{DynamicResource ButtonElementDark}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="3*"/>
                                <ColumnDefinition Width="60"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="текст" VerticalAlignment="Center" Margin="40,0,40,10"/>
                            <TextBlock Grid.Column="1" VerticalAlignment="Center">
                                <Image Source="/View/Images/SeparatorDark.png"/>
                            </TextBlock>
                            <TextBlock Grid.Column="2" Text="10/01/2026" VerticalAlignment="Center" />
                        </Grid>
                    </Button>
                </ListBoxItem>
                <ListBoxItem Style="{StaticResource ListBoxItemForListItems}">
                    <Button x:Name="AddItemButton2" Style="{StaticResource ButtonPlus}" 
                            Click="AddItemButton2_Click">
                        <Image Source="/View/Images/Plus.png"/>
                    </Button>
                </ListBoxItem>
            </ListBox>
        </Grid>
    </Border>
</Window>
